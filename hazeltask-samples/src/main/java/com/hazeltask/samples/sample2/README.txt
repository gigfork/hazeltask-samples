This example is to illustrate the default round robining of tasks.

To run:
1) Start 2 or more workers
2) wait until they are started
3) Start 1 producer

The producer will output which workers executed the tasks it created.